import uniproxy.rules.Rule

// object RuleTests extends munit.FunSuite:

//   test("hello") {
//     val rule = Rule("domain", "google.com", "direct")
//     assertEquals(rule.toString, "Rule(domain,google.com,direct)")
//   }
